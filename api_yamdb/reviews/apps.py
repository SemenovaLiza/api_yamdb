from django.apps import AppConfig


class ReviewsConfig(AppConfig):
    """Base configuration class for the Reviews app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'reviews'
